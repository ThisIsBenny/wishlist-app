name: 'CI'

on:
  push:
    branches:
      - main
      - 'release/*'
    tags-ignore: ['v*']
  pull_request:

jobs:
  call-shared-ci:
    uses: ./.github/workflows/shared.yml
    with:
      run_docker_test: true
